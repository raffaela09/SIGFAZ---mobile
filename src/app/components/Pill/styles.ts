import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  pill: {
    height: 30,
    borderRadius: 30,
    paddingHorizontal: 16, // Espaçamento lateral dinâmico
    alignItems: "center",
    justifyContent: "center",
  },
});

export default styles;
